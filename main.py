import gui

def main():
    gui.gui_init()
    # # Replace 'path/to/your/credentials.json' with the actual path to your credentials JSON file
    # credentials_file_path = 'path/to/your/credentials.json'

    # # Replace 'YOUR_SPREADSHEET_ID' with the ID of the Google Sheets document where you want to create the worksheet
    # spreadsheet_id = 'YOUR_SPREADSHEET_ID'

    # # Replace 'New Worksheet' with the name you want to give to the new worksheet
    # worksheet_name = 'New Worksheet'

    # # You can adjust the rows and columns as per your requirement
    # rows = 10000
    # cols = 1000

    # create_new_worksheet(credentials_file_path, spreadsheet_id, worksheet_name, rows=rows, cols=cols)

if __name__ == "__main__":
    main()